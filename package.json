{
  "name": "cap2",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "dependencies": {
    "@sap/cds": "^8.9.3",
    "@sap/cds-dk": "^8.9.2",
    "@sap/hana-client": "^2.24.24",
    "express": "^4",
    "hana-cli": "^3.202504.1",
    "strace": "^0.0.1"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cds build",
    "deploy": "cds deploy",
    "start": "cds run",
    "env": "cp default-env.json db && cp default-env.json srv"
  },
  "cds": {
    "build": {
      "target": "."
    },
    "requires": {
      "db": {
        "kind": "hana",
        "model": [
          "db",
          "srv"
        ]
      },
      "hana": {
        "deploy-format": "hdbtable"
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "files": [
    "db",
    "srv"
  ]
}
